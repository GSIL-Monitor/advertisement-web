<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="orderComment">

	<resultMap id="orderCommentMap" type="OrderComment">
		<id property="orderCommentId" column="order_comment_id" jdbcType="NUMERIC" />
		<result property="orderId" column="order_id" jdbcType="NUMERIC" />
		<result property="userId" column="user_id" jdbcType="VARCHAR" />
		<result property="userCommentTags" column="user_comment_tags" jdbcType="VARCHAR" />
		<result property="orderCommentTags" column="order_comment_tags" jdbcType="VARCHAR" />
		<result property="userSatisfactionLevel" column="user_satisfaction_level" jdbcType="NUMERIC" />
		<result property="orderSatisfactionLevel" column="order_satisfaction_level" jdbcType="NUMERIC" />
		<result property="userInfoEvaluate" column="user_info_evaluate" jdbcType="VARCHAR" />
		<result property="orderInfoEvaluate" column="order_info_evaluate" jdbcType="VARCHAR" />
		<result property="status" column="status" jdbcType="NUMERIC" />
		<result property="createTime" column="create_time" jdbcType="TIMESTAMP" />
		<result property="updateTime" column="update_time" jdbcType="TIMESTAMP" />
	</resultMap>

	<!-- 查询sql -->
	<select id="selectOrderComments" parameterType="OrderComment" resultMap="orderCommentMap">
		select
		* from
		tb_order_comment
		<where>
			<trim suffixOverrides="and">
				<if test="orderCommentId != null">
					order_comment_id= #{orderCommentId} and
				</if>
				<if test="orderId != null">
					order_id= #{orderId} and
				</if>
				<if test="userId != null">
					user_id= #{userId} and
				</if>
				<if test="userCommentTags != null">
					user_comment_tags= #{userCommentTags} and
				</if>
				<if test="orderCommentTags != null">
					order_comment_tags= #{orderCommentTags} and
				</if>
				<if test="userSatisfactionLevel != null">
					user_satisfaction_level= #{userSatisfactionLevel} and
				</if>
				<if test="orderSatisfactionLevel != null">
					order_satisfaction_level= #{orderSatisfactionLevel} and
				</if>
				<if test="userInfoEvaluate != null">
					user_info_evaluate= #{userInfoEvaluate} and
				</if>
				<if test="orderInfoEvaluate != null">
					order_info_evaluate= #{orderInfoEvaluate} and
				</if>
				<if test="status != null">
					status= #{status} and
				</if>
			</trim>
			and status != -1
		</where>
		order by create_time desc
	</select>

	<select id="selectOrderCommentByIds" parameterType="List" resultMap="orderCommentMap">
		select
		* from
		tb_order_comment
		<where>
			order_comment_id in
			<foreach item="id" collection="list" open="(" separator=","
				close=")">
				 #{id}
			</foreach>
		</where>
	</select>

	<insert id="insertOrderComment" parameterType="OrderComment">
	insert into
	tb_order_comment( order_id, user_id, user_comment_tags, order_comment_tags,
	user_satisfaction_level, order_satisfaction_level, user_info_evaluate,
	order_info_evaluate, status, create_time, update_time )
	values
	( #{orderId}, #{userId}, #{userCommentTags}, #{orderCommentTags},
	#{userSatisfactionLevel}, #{orderSatisfactionLevel},
	#{userInfoEvaluate}, #{orderInfoEvaluate}, #{status}, sysdate(),
	sysdate() )
	<selectKey resultType="java.lang.Long" order="AFTER"
		keyProperty="orderCommentId">
		SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>

	<update id="updateOrderComment" parameterType="OrderComment">
		update tb_order_comment
		<set>
			<trim suffixOverrides=",">
				<if test="orderId != null">
					order_id= #{orderId},
				</if>
				<if test="userId != null">
					user_id= #{userId},
				</if>
				<if test="userCommentTags != null">
					user_comment_tags= #{userCommentTags},
				</if>
				<if test="orderCommentTags != null">
					order_comment_tags= #{orderCommentTags},
				</if>
				<if test="userSatisfactionLevel != null">
					user_satisfaction_level= #{userSatisfactionLevel},
				</if>
				<if test="orderSatisfactionLevel != null">
					order_satisfaction_level= #{orderSatisfactionLevel},
				</if>
				<if test="userInfoEvaluate != null">
					user_info_evaluate= #{userInfoEvaluate},
				</if>
				<if test="orderInfoEvaluate != null">
					order_info_evaluate= #{orderInfoEvaluate},
				</if>
				<if test="status != null">
					status= #{status},
				</if>
				update_time=sysdate(),
			</trim>
		</set>
		<where>
			order_comment_id=#{orderCommentId}
		</where>
	</update>

	<delete id="deleteOrderComment" parameterType="long">
		delete from tb_order_comment
		where
		order_comment_id=#{orderCommentId}
	</delete>

</mapper>